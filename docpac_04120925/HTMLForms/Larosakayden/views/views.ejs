<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <script>
        data = `<%- JSON.stringify(data) %>`;
        data = JSON.parse(data);
        console.log(data);

        data.forEach(item => {
            document.write(`<p>${item.username}'s favorite game is ${item.fgame}.</p>`);
        });
    </script>

    <a href="/add">add</a>
    <a href="/">home</a>
</body>
</html>